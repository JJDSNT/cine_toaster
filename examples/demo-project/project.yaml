# A Cine Toaster production is a directory of YAML the runtime reads directly.
# There is no import step and no generated copy: these files are the production.
schema_version: 1
id: the-last-signal
title: The Last Signal
format: Short film · 8 minutes
logline: A lone radio astronomer hears a reply hidden inside the final broadcast from Earth.

paths:
  scenes: scenes
  script: story/screenplay.fountain

production:
  phase: production
  active_scene: SC-030

sequences:
  - id: the-reply
    label: The Reply
    act: Act II
    scenes: ["SC-010", "SC-030"]
